{
  "openapi": "3.1.0",
  "info": {
    "title": "arkhe",
    "summary": "ARK identifier infrastructure — minter and resolver as separate services",
    "description": "Minting and resolution of ARK identifiers.\n\n**An ARK is never re-assigned (NR).** That one commitment shapes most of this API.\n\n* **A minted ARK cannot be withdrawn.** There is no delete; when an object is lost you\n  `tombstone` it (the description stays, only reachability goes).\n* **A resend does not mint again.** Send a `request_id` and the same value resent\n  returns the same ARK. Batches of tens of thousands are interrupted more often than\n  not, so an interrupted batch can simply be sent again.\n* **Naming a shoulder in a request does not widen anything.** Reach comes from the\n  credential's registration; omit it and the organisation's default is used.\n* **Child resources are not minted.** A deep reference such as\n  `ark:99999/x9tn1qkq2g7/page/3` is covered by suffix passthrough, so one record per\n  minting is enough.\n\n### Authentication\n\nPut a bearer token in via `Authorize`. Which credentials are accepted is set at start-up\nby `ARKHE_AUTH`: an API key, a token arkhe issued, or a JWT from an external\nauthorisation server (any combination).\n\n**Reading public information needs no authentication**, because a repository shows its\npublic records to anyone.\n",
    "license": {
      "name": "MIT",
      "identifier": "MIT"
    },
    "version": "0.2.0"
  },
  "paths": {
    "/.well-known/ark": {
      "get": {
        "tags": [
          "resolve"
        ],
        "summary": "Well Known Ark",
        "description": "**Tells a client that this host has an ARK resolver** (draft-kunze-ark-42 §5.6).\n\n§5.6 registers `ark` in the Well-Known URIs registry (RFC 8615) and defines the answer\nas **plain text holding the resolver's root path, ending in `/`** — append a compact ARK\nto it and you have a resolution request. **A client that sends no `Accept`, or `*/*`,\ngets that**; answering such a client with JSON would make the host look, to anyone\nreading the specification, as though it had no ARK resolver at all.\n\n`Accept: application/json` returns arkhe's own inventory instead: **where to go when a\nNAAN's minting happens elsewhere** (`Naan.minter` / `Shoulder.minter`), and any\nnamespace whose redirection is on hold.\n\nBoth representations carry `Vary: Accept`.",
        "operationId": "well_known_ark__well_known_ark_get",
        "responses": {
          "200": {
            "description": "`text/plain` by default: the resolver's root path, one line (draft-kunze-ark-42 §5.6). `Accept: application/json` returns the namespaces this ledger holds.",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              },
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          }
        }
      }
    },
    "/ark:{rest}": {
      "get": {
        "tags": [
          "resolve"
        ],
        "summary": "Resolve Ark",
        "description": "**Resolve an ARK. No authentication.** Both `ark:99999/x9tn1qkq2g7` and the older\n`ark:/99999/x9tn1qkq2g7` are accepted, in any letter case.\n\nThere is more than one way to answer, and **keeping the identifier alive comes first on\nevery path**:\n\n    302  redirect to the target (the usual case; a shoulder's delegation template may\n         name 301, 303 or 307 instead)\n    200  return a description — for `?info` and `??`, for a target a browser cannot\n         open (`urn:isbn:…`), for an empty target, for a tombstone, and while a hold\n         is on\n    404  not in this ledger and nowhere to forward to. **`?info` on an unknown name is\n         still a 404** — there is nothing to say about a name we do not know\n    400  not readable as an ARK\n\n**A hold is not a 404.** The identifier exists; we are only declining to hand out its\naddress for now, so the reason and the expiry come back with a 200. **The same holds for\na tombstone** — saying \"it was lost\" is not the same as saying \"it never was\".\n\nAn ARK that is only a NAAN (`ark:12345`) answers with what can be said about that NAAN.\nAn unknown NAAN is forwarded to the global resolver (`ARKHE_GLOBAL_RESOLVER`, n2t.net by\ndefault).\n\nEvery answer this resolver gives about an identifier carries the THUMP headers of §5.2\n(`THUMP-Status` and `Link: <…>; rel=\"describes\"`); redirects carry neither.",
        "operationId": "resolve_ark_ark__rest__get",
        "parameters": [
          {
            "name": "rest",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Rest"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "a description (`?info` / `?` / `??` / `?json`, a target that cannot be opened, a tombstone, a hold, an ARK that is only a NAAN)",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              },
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              },
              "text/html": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "301": {
            "description": "redirect to the target (a delegation template named `301 `)"
          },
          "302": {
            "description": "redirect to the target (the default)"
          },
          "303": {
            "description": "redirect to the target (a delegation template named `303 `)"
          },
          "307": {
            "description": "redirect to the target (a delegation template named `307 `)"
          },
          "400": {
            "description": "not readable as an ARK",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "404": {
            "description": "not in this ledger, and nowhere to forward to",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/ark:/{rest}": {
      "get": {
        "tags": [
          "resolve"
        ],
        "summary": "Resolve Ark",
        "description": "**Resolve an ARK. No authentication.** Both `ark:99999/x9tn1qkq2g7` and the older\n`ark:/99999/x9tn1qkq2g7` are accepted, in any letter case.\n\nThere is more than one way to answer, and **keeping the identifier alive comes first on\nevery path**:\n\n    302  redirect to the target (the usual case; a shoulder's delegation template may\n         name 301, 303 or 307 instead)\n    200  return a description — for `?info` and `??`, for a target a browser cannot\n         open (`urn:isbn:…`), for an empty target, for a tombstone, and while a hold\n         is on\n    404  not in this ledger and nowhere to forward to. **`?info` on an unknown name is\n         still a 404** — there is nothing to say about a name we do not know\n    400  not readable as an ARK\n\n**A hold is not a 404.** The identifier exists; we are only declining to hand out its\naddress for now, so the reason and the expiry come back with a 200. **The same holds for\na tombstone** — saying \"it was lost\" is not the same as saying \"it never was\".\n\nAn ARK that is only a NAAN (`ark:12345`) answers with what can be said about that NAAN.\nAn unknown NAAN is forwarded to the global resolver (`ARKHE_GLOBAL_RESOLVER`, n2t.net by\ndefault).\n\nEvery answer this resolver gives about an identifier carries the THUMP headers of §5.2\n(`THUMP-Status` and `Link: <…>; rel=\"describes\"`); redirects carry neither.",
        "operationId": "resolve_ark_ark___rest__get",
        "parameters": [
          {
            "name": "rest",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Rest"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "a description (`?info` / `?` / `??` / `?json`, a target that cannot be opened, a tombstone, a hold, an ARK that is only a NAAN)",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              },
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              },
              "text/html": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "301": {
            "description": "redirect to the target (a delegation template named `301 `)"
          },
          "302": {
            "description": "redirect to the target (the default)"
          },
          "303": {
            "description": "redirect to the target (a delegation template named `303 `)"
          },
          "307": {
            "description": "redirect to the target (a delegation template named `307 `)"
          },
          "400": {
            "description": "not readable as an ARK",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "404": {
            "description": "not in this ledger, and nowhere to forward to",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "HTTPValidationError": {
        "properties": {
          "detail": {
            "items": {
              "$ref": "#/components/schemas/ValidationError"
            },
            "type": "array",
            "title": "Detail"
          }
        },
        "type": "object",
        "title": "HTTPValidationError"
      },
      "ValidationError": {
        "properties": {
          "loc": {
            "items": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "integer"
                }
              ]
            },
            "type": "array",
            "title": "Location"
          },
          "msg": {
            "type": "string",
            "title": "Message"
          },
          "type": {
            "type": "string",
            "title": "Error Type"
          },
          "input": {
            "title": "Input"
          },
          "ctx": {
            "type": "object",
            "title": "Context"
          }
        },
        "type": "object",
        "required": [
          "loc",
          "msg",
          "type"
        ],
        "title": "ValidationError"
      }
    }
  },
  "tags": [
    {
      "name": "ark",
      "description": "Minting and updating. **A write never reaches outside the caller's registered reach.**"
    },
    {
      "name": "resolve",
      "description": "Resolution, with the `?` (a brief description), `??` (the persistence statement), `?info` (for a person) and `?json` (for a program) inflections. **A description can be answered even when the object cannot be reached** (FAIR A2). Present only when started with `ARKHE_RESOLVER=1`."
    }
  ]
}
