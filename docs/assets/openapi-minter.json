{
  "openapi": "3.1.0",
  "info": {
    "title": "arkhe",
    "summary": "ARK identifier infrastructure — minter and resolver as separate services",
    "description": "Minting and resolution of ARK identifiers.\n\n**An ARK is never re-assigned (NR).** That one commitment shapes most of this API.\n\n* **A minted ARK cannot be withdrawn.** There is no delete; when an object is lost you\n  `tombstone` it (the description stays, only reachability goes).\n* **A resend does not mint again.** Send a `request_id` and the same value resent\n  returns the same ARK. Batches of tens of thousands are interrupted more often than\n  not, so an interrupted batch can simply be sent again.\n* **Naming a shoulder in a request does not widen anything.** Reach comes from the\n  credential's registration; omit it and the organisation's default is used.\n* **Child resources are not minted.** A deep reference such as\n  `ark:99999/x9tn1qkq2g7/page/3` is covered by suffix passthrough, so one record per\n  minting is enough.\n\n### Authentication\n\nPut a bearer token in via `Authorize`. Which credentials are accepted is set at start-up\nby `ARKHE_AUTH`: an API key, a token arkhe issued, or a JWT from an external\nauthorisation server (any combination).\n\n**Reading public information needs no authentication**, because a repository shows its\npublic records to anyone.\n",
    "license": {
      "name": "MIT",
      "identifier": "MIT"
    },
    "version": "0.9.0"
  },
  "paths": {
    "/oauth/token": {
      "post": {
        "tags": [
          "oauth"
        ],
        "summary": "Issue an access token (client_credentials)",
        "description": "RFC 6749 §4.4 client_credentials.\n\nCredentials are accepted **in the body or by Basic authentication** (§2.3.1 recommends Basic and permits the body; client libraries in the wild use both).",
        "operationId": "token_oauth_token_post",
        "requestBody": {
          "content": {
            "application/x-www-form-urlencoded": {
              "schema": {
                "$ref": "#/components/schemas/Body_token_oauth_token_post"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {}
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/mint": {
      "post": {
        "tags": [
          "ark"
        ],
        "summary": "Mint",
        "description": "**Mint one new ARK.** Requires `ark:mint`.\n\nThe shoulder defaults to the organisation's own. Naming one in the request only asks\nwhether it lies inside the caller's registered reach — it never widens it.\n\n**Send a `request_id` and a resend will not mint again.** The same principal resending\nthe same `request_id` gets back the ARK minted the first time, so a lost response does\nnot leave a dead identifier behind. The status code says which happened:\n\n    201  minted\n    200  returned an earlier minting (a resend)\n\n**An ARK is never reissued**, so minting cannot be undone.",
        "operationId": "mint_api_mint_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MintIn"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ArkOut"
                }
              }
            }
          },
          "200": {
            "description": "returned an earlier minting (a resend)",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ArkOut"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        },
        "security": [
          {
            "oauth2": [
              "ark:mint"
            ]
          },
          {
            "bearer": []
          }
        ]
      }
    },
    "/api/mint/bulk": {
      "post": {
        "tags": [
          "ark"
        ],
        "summary": "Bulk Mint",
        "description": "**Mint in bulk.** Requires `ark:mint`. One request holds at most `ARKHE_BULK_LIMIT`\nrows (1000 by default).\n\n**One row out of reach and nothing is created.** Reach and shoulder are checked for\nevery row before any minting, so a half-minted batch cannot be left behind.\n\n**The answer keeps the order of the input**, because resent rows (a `request_id`\nalready seen) and new ones are mixed and the caller has to line them up. `created` and\n`replayed` carry the two counts; all resends answer 200, one new minting makes it 201.\n\nGive each row a `request_id` and **an interrupted batch can be sent again as it is** —\nrows already minted are skipped. **The same `request_id` twice inside one request still\nmints once**: it is one request, so it gets one number.",
        "operationId": "bulk_mint_api_mint_bulk_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/BulkMintIn"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/BulkMintOut"
                }
              }
            }
          },
          "200": {
            "description": "every row was a resend",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/BulkMintOut"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        },
        "security": [
          {
            "oauth2": [
              "ark:mint"
            ]
          },
          {
            "bearer": []
          }
        ]
      }
    },
    "/api/register": {
      "post": {
        "tags": [
          "ark"
        ],
        "summary": "Register",
        "description": "**Register a row for a qualified ARK** — an existing base name plus a qualifier.\n\nSuffix passthrough already covers a reference of any depth; this endpoint **overrides\nthat default at a single point**: \"this subtree lives in another store\", \"this\nderivative sits elsewhere\".\n\n**It requires `ark:mint`.** Nothing is minted, but a new resolvable identifier does\nappear, so it must not be handed to a principal that holds update rights alone.",
        "operationId": "register_api_register_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RegisterIn"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ArkOut"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        },
        "security": [
          {
            "oauth2": [
              "ark:mint"
            ]
          },
          {
            "bearer": []
          }
        ]
      }
    },
    "/api/import": {
      "post": {
        "tags": [
          "ark"
        ],
        "summary": "Import Ark",
        "description": "**Record an ARK that was minted elsewhere.** Requires `ark:import`.\n\nMinting does not let a caller choose the name; this does, and that single difference is\nwhy it has its own scope and its own checks. It exists so that **a name minted inside a\nclosed network can later be published under the same identifier** — without it, opening\nan embargoed object means issuing a different ARK, and every reference handed out while\nit was closed dies.\n\nThree things are checked, and none of them can be waived:\n\n* the shoulder is **delegated** — importing into a namespace this ledger mints in could\n  collide with its own minter, and a name minted elsewhere only exists because the\n  namespace was delegated in the first place;\n* the name falls **inside that shoulder**;\n* the **check digit verifies** — for a name that arrives from outside, that is the only\n  evidence there is that it was not mistyped.\n\nA name already in the ledger is refused rather than overwritten, exactly as minting is.",
        "operationId": "import_ark_api_import_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ImportIn"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ArkOut"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        },
        "security": [
          {
            "oauth2": [
              "ark:import"
            ]
          },
          {
            "bearer": []
          }
        ]
      }
    },
    "/api/import/bulk": {
      "post": {
        "tags": [
          "ark"
        ],
        "summary": "Bulk Import",
        "description": "**Import a whole delegated namespace at once.** Requires `ark:import`. One request holds\nat most `ARKHE_BULK_LIMIT` rows.\n\nThis is the shape the closed-network case actually needs: a delegate hands over the\nnames it minted, and they arrive together. Each row goes through exactly the same checks\nas a single import, and **one row that fails any of them means nothing is created** — a\nhalf-imported namespace is worse than none, because the names that did land cannot be\ntaken back.\n\nRows may span several shoulders, as long as every one of them is delegated and within\nthe caller's reach.",
        "operationId": "bulk_import_api_import_bulk_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/BulkImportIn"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/BulkImportOut"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        },
        "security": [
          {
            "oauth2": [
              "ark:import"
            ]
          },
          {
            "bearer": []
          }
        ]
      }
    },
    "/api/update": {
      "put": {
        "tags": [
          "ark"
        ],
        "summary": "Update",
        "description": "Update an existing ARK. **The manager of the target's shoulder is checked.**",
        "operationId": "update_api_update_put",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateIn"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ArkOut"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        },
        "security": [
          {
            "oauth2": [
              "ark:update"
            ]
          },
          {
            "bearer": []
          }
        ]
      },
      "patch": {
        "tags": [
          "ark"
        ],
        "summary": "Patch",
        "description": "**Update the fields you send, and leave the rest alone.** Requires `ark:update`.\n\n`PUT` replaces the record: every field you omit takes its default, so repointing an ARK\nwith `{\"ark\": …, \"url\": …}` alone **empties its title, its who and its when**. That is\ncorrect for a replacement and wrong for what people actually do most of the time, which\nis move an object.\n\nSending a field as `\"\"` clears it; not sending it leaves it untouched. Both are needed,\nwhich is why this is not simply \"ignore empty strings\".",
        "operationId": "patch_api_update_patch",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PatchIn"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ArkOut"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        },
        "security": [
          {
            "oauth2": [
              "ark:update"
            ]
          },
          {
            "bearer": []
          }
        ]
      }
    },
    "/api/update/bulk": {
      "put": {
        "tags": [
          "ark"
        ],
        "summary": "Bulk Update",
        "description": "Update in bulk. Rows are matched by key, and **nothing is applied unless every row\nis found and in reach** — there is no partial application.",
        "operationId": "bulk_update_api_update_bulk_put",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/BulkUpdateIn"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/BulkUpdateOut"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        },
        "security": [
          {
            "oauth2": [
              "ark:update"
            ]
          },
          {
            "bearer": []
          }
        ]
      }
    },
    "/api/publish": {
      "post": {
        "tags": [
          "ark"
        ],
        "summary": "Publish",
        "description": "**Publish an ARK.** Requires `ark:mint`.\n\nUntil it is published an ARK does not resolve and can be deleted outright; from here on\nit resolves, and deleting it means first withdrawing it from publication.\n\nThis also **publishes again** an ARK that was withdrawn. The record keeps the moment it\nfirst went out (`first_published_at`), which never changes: what has been out in the\nworld cannot be made never to have been out.\n\nPublishing twice is not an error — the second call returns the same record, so a lost\nresponse can simply be resent.",
        "operationId": "publish_api_publish_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PublishIn"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ArkOut"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        },
        "security": [
          {
            "oauth2": [
              "ark:mint"
            ]
          },
          {
            "bearer": []
          }
        ]
      }
    },
    "/api/unpublish": {
      "post": {
        "tags": [
          "ark"
        ],
        "summary": "Unpublish",
        "description": "**Withdraw an ARK from publication.** Requires `ark:unpublish`, and the ARK must be\nwithin your reach — an organisation's own shoulder, a NAAN administrator's own NAAN,\neverything for the registration authority.\n\nThe record stays and **can be published again**; in the meantime the identifier does not\nresolve. It is the reversible half: deleting the record is a separate call, and that one\ndoes not come back.\n\n**`NR` is not broken by this.** A withdrawn name belongs to nobody and there is no path\nanywhere that reassigns it, so a stale reference gets `404` — never a different object.\nWhat breaks is \"keeps resolving\", not \"never means something else\".\n\nBecause the name **has been published**, two things are required: a `reason` (someone may\nbe citing it already, and there is no way to know that from here) and `confirm` repeating\nthe ARK (so that a script walking a list cannot withdraw everything by accident).",
        "operationId": "unpublish_api_unpublish_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UnpublishIn"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ArkOut"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        },
        "security": [
          {
            "oauth2": [
              "ark:unpublish"
            ]
          },
          {
            "bearer": []
          }
        ]
      }
    },
    "/api/delete": {
      "post": {
        "tags": [
          "ark"
        ],
        "summary": "Delete Ark",
        "description": "**Delete an ARK that has not been published.** Requires `ark:delete`.\n\nThis is the one case where an ARK goes away. NR (no re-assignment) binds the names that\nwere **put out into the world**; a reserved one, minted for an object whose publication\nwas then abandoned, is better removed than left pointing at nothing forever.\n\nA published ARK is refused with `409` — tombstone it instead. So is one that still has\nqualified names under it: withdraw those first.\n\n**The name is not freed.** It is kept in the ledger of withdrawn names and never\nassigned again, because a reserved identifier has usually already been handed to\nsomeone — re-using it would be indistinguishable, from the outside, from breaking NR.",
        "operationId": "delete_ark_api_delete_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DeleteIn"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DeleteOut"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        },
        "security": [
          {
            "oauth2": [
              "ark:delete"
            ]
          },
          {
            "bearer": []
          }
        ]
      }
    },
    "/api/stats": {
      "get": {
        "tags": [
          "ark"
        ],
        "summary": "Ledger Stats",
        "description": "**Counts for the ledger as you see it.** Requires `ark:read`.\n\nNothing outside your reach is included — an organisation sees its own shoulders, a NAAN\nadministrator its NAAN, the registration authority everything. **A total is itself a\ndisclosure**: how many identifiers an organisation holds is that organisation's business,\nso the same reach that limits the listing limits this.\n\n`withdrawn_after_publication` is kept apart from `withdrawn` on purpose. Retracting a\nreservation nobody saw and removing a name that was out in the world are different acts,\nand **the second one is the number of times the promise was broken.** It does not belong\nhidden inside a total.\n\n**Counting is exact, so it costs time proportional to the number of rows.** The listing\navoids counting because it only needs to know whether there is more; here the number *is*\nthe answer. What can be avoided is the number of passes: every figure over the same set is\ngathered with conditional aggregation, so **the ark table is read twice, not seven times**\n— which also keeps the figures consistent with each other, since counts taken separately\ndrift apart as rows arrive. Measured at about 110 ms over 300,000 ARKs. **This is not an\nendpoint to poll every second.**",
        "operationId": "ledger_stats_api_stats_get",
        "security": [
          {
            "oauth2": [
              "ark:read"
            ]
          },
          {
            "bearer": []
          }
        ],
        "parameters": [
          {
            "name": "naan",
            "in": "query",
            "required": false,
            "schema": {
              "type": "string",
              "default": "",
              "title": "Naan"
            }
          },
          {
            "name": "org",
            "in": "query",
            "required": false,
            "schema": {
              "type": "string",
              "default": "",
              "title": "Org"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/StatsOut"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/api/purge": {
      "post": {
        "tags": [
          "ark"
        ],
        "summary": "Purge",
        "description": "**Purge a published ARK** — unpublish and delete in one step. Requires `ark:purge`, and\nthe ARK must be within your reach.\n\nEverything else in this service exists so that a published identifier keeps resolving.\nThis endpoint is the one way out, and it is here because **reality sometimes brings a\ndemand that outweighs an identifier** — a removal order, personal data that should never\nhave been published, a mass ingest that went in wrong. Without a way out, someone ends up\ndeleting rows straight from the database, and **a deletion that leaves no trace is the\nworst kind.**\n\nSo the way is narrow and it is recorded:\n\n* **within your own reach only** — an organisation cannot touch another's shoulder;\n* **a reason is required**, and it is kept with the name;\n* **`confirm` must repeat the ARK**, so that a script walking a list cannot empty the\n  ledger by accident;\n* **the name is not freed** — it is never assigned again.\n\nThat last point is what survives: the target and the description go, but **the name can\nnever come to mean something else**. A stale reference gets `404`; it never gets a\ndifferent object.",
        "operationId": "purge_api_purge_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PurgeIn"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DeleteOut"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        },
        "security": [
          {
            "oauth2": [
              "ark:purge"
            ]
          },
          {
            "bearer": []
          }
        ]
      }
    },
    "/api/tombstone": {
      "put": {
        "tags": [
          "ark"
        ],
        "summary": "Tombstone",
        "description": "**Declare that the object is gone.** The ARK is not deleted.\n\nUnder NR (no re-assignment) an identifier cannot be removed; only reachability can be.\n**The identifier and its metadata stay**, and the resolver answers with the description.\n\n**Its scope is separate from `ark:update`.** A tombstone says \"this is gone\", not \"this\nis elsewhere\" — a different meaning with different consequences. It is hard to walk back\nand it is public, so it does not belong to routine writers such as an ingest batch.",
        "operationId": "tombstone_api_tombstone_put",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TombstoneIn"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ArkOut"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        },
        "security": [
          {
            "oauth2": [
              "ark:tombstone"
            ]
          },
          {
            "bearer": []
          }
        ]
      }
    },
    "/api/hold": {
      "put": {
        "tags": [
          "ark"
        ],
        "summary": "Hold",
        "description": "**Stop redirecting, temporarily.** Resolution is not stopped — the description keeps\nbeing returned.\n\nFor when a delegate is down, a wrong target went out, or an object is moving: you have\nto stop quickly without killing the identifier. `404` would be a lie (the identifier\nexists) and `503` makes a permanent identifier look broken, so a hold answers **200 with\nthe description**, the same path a tombstone takes.\n\n**Its scope is separate from `ark:update`**, because stopping is a different decision\nfrom repointing and the reason is published. It is separate from a tombstone too: that\nis a permanent declaration, while this is **dated, and keeps the previous target**.",
        "operationId": "hold_api_hold_put",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/HoldIn"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ArkOut"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        },
        "security": [
          {
            "oauth2": [
              "ark:hold"
            ]
          },
          {
            "bearer": []
          }
        ]
      }
    },
    "/api/hold/release": {
      "put": {
        "tags": [
          "ark"
        ],
        "summary": "Hold Release",
        "description": "Lift a hold before its expiry. **An expired hold lifts itself by the clock**, so this\nis only for lifting one early.",
        "operationId": "hold_release_api_hold_release_put",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/HoldReleaseIn"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ArkOut"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        },
        "security": [
          {
            "oauth2": [
              "ark:hold"
            ]
          },
          {
            "bearer": []
          }
        ]
      }
    },
    "/api/query": {
      "post": {
        "tags": [
          "ark"
        ],
        "summary": "Bulk Query",
        "description": "Look up ARKs in bulk. **Reads are confined to the caller's reach**, exactly as writes\nare.",
        "operationId": "bulk_query_api_query_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/BulkQueryIn"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/BulkQueryOut"
                }
              }
            }
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        },
        "security": [
          {
            "oauth2": [
              "ark:read"
            ]
          },
          {
            "bearer": []
          }
        ]
      }
    }
  },
  "components": {
    "schemas": {
      "ArkOut": {
        "properties": {
          "ark": {
            "type": "string",
            "title": "Ark"
          },
          "url": {
            "type": "string",
            "title": "Url",
            "default": ""
          },
          "title": {
            "type": "string",
            "title": "Title",
            "default": ""
          },
          "type": {
            "type": "string",
            "title": "Type",
            "default": ""
          },
          "identifier": {
            "type": "string",
            "title": "Identifier",
            "default": ""
          },
          "format": {
            "type": "string",
            "title": "Format",
            "default": ""
          },
          "relation": {
            "type": "string",
            "title": "Relation",
            "default": ""
          },
          "source": {
            "type": "string",
            "title": "Source",
            "default": ""
          },
          "commitment": {
            "type": "string",
            "title": "Commitment",
            "default": ""
          },
          "metadata": {
            "type": "string",
            "title": "Metadata",
            "default": ""
          },
          "who": {
            "type": "string",
            "title": "Who",
            "default": ""
          },
          "when": {
            "type": "string",
            "title": "When",
            "default": ""
          },
          "created_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Created At"
          },
          "updated_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Updated At"
          },
          "published_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Published At"
          },
          "hold_until": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Hold Until"
          },
          "hold_reason": {
            "type": "string",
            "title": "Hold Reason",
            "default": ""
          }
        },
        "type": "object",
        "required": [
          "ark"
        ],
        "title": "ArkOut"
      },
      "Body_token_oauth_token_post": {
        "properties": {
          "grant_type": {
            "type": "string",
            "title": "Grant Type",
            "default": ""
          },
          "client_id": {
            "type": "string",
            "title": "Client Id",
            "default": ""
          },
          "client_secret": {
            "type": "string",
            "title": "Client Secret",
            "default": ""
          },
          "scope": {
            "type": "string",
            "title": "Scope",
            "default": ""
          }
        },
        "type": "object",
        "title": "Body_token_oauth_token_post"
      },
      "BulkImportIn": {
        "properties": {
          "data": {
            "items": {
              "$ref": "#/components/schemas/ImportIn"
            },
            "type": "array",
            "title": "Data"
          }
        },
        "type": "object",
        "required": [
          "data"
        ],
        "title": "BulkImportIn",
        "description": "Import in bulk. **One row that fails any check and nothing is created** — a half-imported namespace is worse than none, because the names that did land cannot be taken back."
      },
      "BulkImportOut": {
        "properties": {
          "imported": {
            "items": {
              "$ref": "#/components/schemas/ArkOut"
            },
            "type": "array",
            "title": "Imported"
          },
          "count": {
            "type": "integer",
            "title": "Count"
          }
        },
        "type": "object",
        "required": [
          "imported",
          "count"
        ],
        "title": "BulkImportOut"
      },
      "BulkMintIn": {
        "properties": {
          "data": {
            "items": {
              "$ref": "#/components/schemas/MintIn"
            },
            "type": "array",
            "title": "Data"
          }
        },
        "type": "object",
        "required": [
          "data"
        ],
        "title": "BulkMintIn"
      },
      "BulkMintOut": {
        "properties": {
          "minted": {
            "items": {
              "$ref": "#/components/schemas/ArkOut"
            },
            "type": "array",
            "title": "Minted"
          },
          "created": {
            "type": "integer",
            "title": "Created"
          },
          "replayed": {
            "type": "integer",
            "title": "Replayed"
          }
        },
        "type": "object",
        "required": [
          "minted",
          "created",
          "replayed"
        ],
        "title": "BulkMintOut"
      },
      "BulkQueryIn": {
        "properties": {
          "data": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Data"
          }
        },
        "type": "object",
        "required": [
          "data"
        ],
        "title": "BulkQueryIn"
      },
      "BulkQueryOut": {
        "properties": {
          "data": {
            "items": {
              "$ref": "#/components/schemas/ArkOut"
            },
            "type": "array",
            "title": "Data"
          }
        },
        "type": "object",
        "required": [
          "data"
        ],
        "title": "BulkQueryOut"
      },
      "BulkUpdateIn": {
        "properties": {
          "data": {
            "items": {
              "$ref": "#/components/schemas/UpdateIn"
            },
            "type": "array",
            "title": "Data"
          }
        },
        "type": "object",
        "required": [
          "data"
        ],
        "title": "BulkUpdateIn"
      },
      "BulkUpdateOut": {
        "properties": {
          "updated": {
            "type": "integer",
            "title": "Updated"
          }
        },
        "type": "object",
        "required": [
          "updated"
        ],
        "title": "BulkUpdateOut"
      },
      "DeleteIn": {
        "properties": {
          "ark": {
            "type": "string",
            "title": "Ark"
          },
          "reason": {
            "type": "string",
            "maxLength": 500,
            "title": "Reason",
            "description": "Why it was withdrawn. Kept with the name; not published. **Required if the ARK has ever been published.**",
            "default": ""
          },
          "confirm": {
            "type": "string",
            "title": "Confirm",
            "description": "The ARK again. **Required if the ARK has ever been published**, so that a script walking a list cannot empty the ledger by accident.",
            "default": ""
          }
        },
        "type": "object",
        "required": [
          "ark"
        ],
        "title": "DeleteIn",
        "description": "Delete an ARK that is not currently published. One that is published must be unpublished first (409), or purged in one step. **If it has ever been published, a reason and `confirm` are required.** The name itself is remembered and never assigned again."
      },
      "DeleteOut": {
        "properties": {
          "ark": {
            "type": "string",
            "title": "Ark"
          },
          "withdrawn_at": {
            "type": "string",
            "format": "date-time",
            "title": "Withdrawn At"
          },
          "reason": {
            "type": "string",
            "title": "Reason",
            "default": ""
          },
          "was_published_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Was Published At"
          }
        },
        "type": "object",
        "required": [
          "ark",
          "withdrawn_at"
        ],
        "title": "DeleteOut",
        "description": "The row is gone; the name is kept so that it is never assigned again."
      },
      "HTTPValidationError": {
        "properties": {
          "detail": {
            "items": {
              "$ref": "#/components/schemas/ValidationError"
            },
            "type": "array",
            "title": "Detail"
          }
        },
        "type": "object",
        "title": "HTTPValidationError"
      },
      "HoldIn": {
        "properties": {
          "ark": {
            "type": "string",
            "title": "Ark"
          },
          "until": {
            "type": "string",
            "format": "date-time",
            "title": "Until",
            "description": "No redirection until this moment. A time in the past is refused."
          },
          "reason": {
            "type": "string",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "description": "Why redirection is stopped. **This is published.**"
          }
        },
        "type": "object",
        "required": [
          "ark",
          "until",
          "reason"
        ],
        "title": "HoldIn",
        "description": "A temporary stop on redirection. **Resolution is not stopped** — the description keeps being returned. `until` is required because a \"temporary\" left to memory becomes permanent; `reason` is required because it is published, and because lifting the hold needs it."
      },
      "HoldReleaseIn": {
        "properties": {
          "ark": {
            "type": "string",
            "title": "Ark"
          }
        },
        "type": "object",
        "required": [
          "ark"
        ],
        "title": "HoldReleaseIn",
        "description": "Lift a hold before its expiry."
      },
      "ImportIn": {
        "properties": {
          "url": {
            "type": "string",
            "title": "Url",
            "default": ""
          },
          "title": {
            "type": "string",
            "title": "Title",
            "default": ""
          },
          "type": {
            "type": "string",
            "title": "Type",
            "default": ""
          },
          "identifier": {
            "type": "string",
            "title": "Identifier",
            "default": ""
          },
          "format": {
            "type": "string",
            "title": "Format",
            "default": ""
          },
          "relation": {
            "type": "string",
            "title": "Relation",
            "default": ""
          },
          "source": {
            "type": "string",
            "title": "Source",
            "default": ""
          },
          "commitment": {
            "type": "string",
            "title": "Commitment",
            "default": ""
          },
          "metadata": {
            "type": "string",
            "title": "Metadata",
            "default": ""
          },
          "who": {
            "type": "string",
            "title": "Who",
            "default": ""
          },
          "when": {
            "type": "string",
            "title": "When",
            "default": ""
          },
          "ark": {
            "type": "string",
            "title": "Ark",
            "description": "The ARK as it was minted elsewhere (`ark:99999/c7962c644f8`)."
          }
        },
        "type": "object",
        "required": [
          "ark"
        ],
        "title": "ImportIn",
        "description": "An ARK minted elsewhere, to be recorded here. Unlike minting, **the caller brings the name**; it must fall inside a delegated shoulder of this ledger and its check digit must verify."
      },
      "MintIn": {
        "properties": {
          "url": {
            "type": "string",
            "title": "Url",
            "default": ""
          },
          "title": {
            "type": "string",
            "title": "Title",
            "default": ""
          },
          "type": {
            "type": "string",
            "title": "Type",
            "default": ""
          },
          "identifier": {
            "type": "string",
            "title": "Identifier",
            "default": ""
          },
          "format": {
            "type": "string",
            "title": "Format",
            "default": ""
          },
          "relation": {
            "type": "string",
            "title": "Relation",
            "default": ""
          },
          "source": {
            "type": "string",
            "title": "Source",
            "default": ""
          },
          "commitment": {
            "type": "string",
            "title": "Commitment",
            "default": ""
          },
          "metadata": {
            "type": "string",
            "title": "Metadata",
            "default": ""
          },
          "who": {
            "type": "string",
            "title": "Who",
            "default": ""
          },
          "when": {
            "type": "string",
            "title": "When",
            "default": ""
          },
          "shoulder": {
            "type": "string",
            "title": "Shoulder",
            "default": ""
          },
          "reserve": {
            "type": "boolean",
            "title": "Reserve",
            "description": "Mint it **without publishing it**. A reserved ARK does not resolve, and it can still be deleted; publish it when the object goes public. The default mints and publishes in one step, as before.",
            "default": false
          },
          "request_id": {
            "type": "string",
            "maxLength": 200,
            "title": "Request Id",
            "description": "Idempotency key. Resending the same value returns the ARK minted the first time, instead of minting again.",
            "default": ""
          }
        },
        "type": "object",
        "title": "MintIn",
        "description": "Input for minting. `shoulder` is optional: omitted, the organisation's default is used; named, it is only checked against the caller's registered reach, never widening it. `naan` is not accepted — it follows from the principal."
      },
      "PatchIn": {
        "properties": {
          "url": {
            "type": "string",
            "title": "Url",
            "default": ""
          },
          "title": {
            "type": "string",
            "title": "Title",
            "default": ""
          },
          "type": {
            "type": "string",
            "title": "Type",
            "default": ""
          },
          "identifier": {
            "type": "string",
            "title": "Identifier",
            "default": ""
          },
          "format": {
            "type": "string",
            "title": "Format",
            "default": ""
          },
          "relation": {
            "type": "string",
            "title": "Relation",
            "default": ""
          },
          "source": {
            "type": "string",
            "title": "Source",
            "default": ""
          },
          "commitment": {
            "type": "string",
            "title": "Commitment",
            "default": ""
          },
          "metadata": {
            "type": "string",
            "title": "Metadata",
            "default": ""
          },
          "who": {
            "type": "string",
            "title": "Who",
            "default": ""
          },
          "when": {
            "type": "string",
            "title": "When",
            "default": ""
          },
          "ark": {
            "type": "string",
            "title": "Ark"
          }
        },
        "type": "object",
        "required": [
          "ark"
        ],
        "title": "PatchIn",
        "description": "Input for a merging update. **Only the fields actually sent are written**; everything else is left as it is. Sending a field as \"\" clears it, which is how a value is removed."
      },
      "PublishIn": {
        "properties": {
          "ark": {
            "type": "string",
            "title": "Ark"
          }
        },
        "type": "object",
        "required": [
          "ark"
        ],
        "title": "PublishIn",
        "description": "Publish an ARK: a reserved one, or one whose publication was withdrawn. From then on it resolves again. Publishing twice is not an error; the second call returns the same record. Deleting it now requires unpublishing it first."
      },
      "PurgeIn": {
        "properties": {
          "ark": {
            "type": "string",
            "title": "Ark"
          },
          "reason": {
            "type": "string",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "description": "Why it is being purged. **Required**: this and the audit log are all that will be left about the identifier."
          },
          "confirm": {
            "type": "string",
            "title": "Confirm",
            "description": "The ARK again, to be sure of the target. Anything else is refused."
          }
        },
        "type": "object",
        "required": [
          "ark",
          "reason",
          "confirm"
        ],
        "title": "PurgeIn",
        "description": "Purge a **published** ARK in one step (unpublish and delete). Within the caller's own reach, for a legal removal order or data that should never have been published. A reason is required and `confirm` must repeat the ARK."
      },
      "RegisterIn": {
        "properties": {
          "url": {
            "type": "string",
            "title": "Url",
            "default": ""
          },
          "title": {
            "type": "string",
            "title": "Title",
            "default": ""
          },
          "type": {
            "type": "string",
            "title": "Type",
            "default": ""
          },
          "identifier": {
            "type": "string",
            "title": "Identifier",
            "default": ""
          },
          "format": {
            "type": "string",
            "title": "Format",
            "default": ""
          },
          "relation": {
            "type": "string",
            "title": "Relation",
            "default": ""
          },
          "source": {
            "type": "string",
            "title": "Source",
            "default": ""
          },
          "commitment": {
            "type": "string",
            "title": "Commitment",
            "default": ""
          },
          "metadata": {
            "type": "string",
            "title": "Metadata",
            "default": ""
          },
          "who": {
            "type": "string",
            "title": "Who",
            "default": ""
          },
          "when": {
            "type": "string",
            "title": "When",
            "default": ""
          },
          "ark": {
            "type": "string",
            "title": "Ark",
            "description": "An existing base ARK (`ark:99999/x9tn1qkq2g7`)."
          },
          "qualifier": {
            "type": "string",
            "title": "Qualifier",
            "description": "Begins with `/` (a part) or `.` (a variant)."
          }
        },
        "type": "object",
        "required": [
          "ark",
          "qualifier"
        ],
        "title": "RegisterIn",
        "description": "A qualified ARK: `ark` is an existing base name and `qualifier` the part reference appended to it. Nothing is minted, so no NOID and no check digit are generated."
      },
      "ShoulderStatOut": {
        "properties": {
          "naan": {
            "type": "string",
            "title": "Naan"
          },
          "shoulder": {
            "type": "string",
            "title": "Shoulder"
          },
          "status": {
            "type": "string",
            "title": "Status"
          },
          "organisation": {
            "type": "string",
            "title": "Organisation",
            "default": ""
          },
          "arks": {
            "type": "integer",
            "title": "Arks"
          },
          "public": {
            "type": "integer",
            "title": "Public"
          },
          "reserved": {
            "type": "integer",
            "title": "Reserved"
          }
        },
        "type": "object",
        "required": [
          "naan",
          "shoulder",
          "status",
          "arks",
          "public",
          "reserved"
        ],
        "title": "ShoulderStatOut",
        "description": "One shoulder's share of the ledger. **The shoulder is the unit the ledger is organised by**, so this is the breakdown that costs nothing extra to produce."
      },
      "StatsOut": {
        "properties": {
          "scope": {
            "type": "string",
            "title": "Scope",
            "description": "How far the caller reaches: system, naan or organisation."
          },
          "naans": {
            "type": "integer",
            "title": "Naans"
          },
          "arks": {
            "type": "integer",
            "title": "Arks"
          },
          "public": {
            "type": "integer",
            "title": "Public"
          },
          "reserved": {
            "type": "integer",
            "title": "Reserved"
          },
          "withdrawn": {
            "type": "integer",
            "title": "Withdrawn"
          },
          "withdrawn_after_publication": {
            "type": "integer",
            "title": "Withdrawn After Publication",
            "description": "Of the withdrawn names, how many had already been published. **This is the number of times the promise was broken**, and it is kept separate for that reason."
          },
          "shoulders": {
            "additionalProperties": {
              "type": "integer"
            },
            "type": "object",
            "title": "Shoulders"
          },
          "organisations": {
            "type": "integer",
            "title": "Organisations"
          },
          "organisations_active": {
            "type": "integer",
            "title": "Organisations Active"
          },
          "clients": {
            "type": "integer",
            "title": "Clients"
          },
          "clients_active": {
            "type": "integer",
            "title": "Clients Active"
          },
          "holds": {
            "additionalProperties": {
              "type": "integer"
            },
            "type": "object",
            "title": "Holds"
          },
          "minted": {
            "additionalProperties": {
              "type": "integer"
            },
            "type": "object",
            "title": "Minted",
            "description": "ARKs minted within the last 24h, 7 days and 30 days."
          },
          "first_mint": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "First Mint"
          },
          "last_mint": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Mint"
          },
          "by_shoulder": {
            "items": {
              "$ref": "#/components/schemas/ShoulderStatOut"
            },
            "type": "array",
            "title": "By Shoulder",
            "default": []
          }
        },
        "type": "object",
        "required": [
          "scope",
          "naans",
          "arks",
          "public",
          "reserved",
          "withdrawn",
          "withdrawn_after_publication",
          "shoulders",
          "organisations",
          "organisations_active",
          "clients",
          "clients_active",
          "holds",
          "minted"
        ],
        "title": "StatsOut",
        "description": "Counts for the ledger **as this caller sees it**. Nothing outside the caller's reach is included — a total is itself a disclosure, since how many identifiers an organisation holds is that organisation's business. Counting is exact and therefore costs time proportional to the number of rows, though every figure over the same set is gathered in one pass: **this is not an endpoint to poll every second.**"
      },
      "TombstoneIn": {
        "properties": {
          "ark": {
            "type": "string",
            "title": "Ark"
          },
          "url": {
            "type": "string",
            "title": "Url",
            "default": ""
          },
          "commitment": {
            "type": "string",
            "title": "Commitment",
            "default": ""
          }
        },
        "type": "object",
        "required": [
          "ark"
        ],
        "title": "TombstoneIn",
        "description": "Declare that the object is gone. **The ARK is not deleted** — the identifier and its metadata stay, only reachability goes."
      },
      "UnpublishIn": {
        "properties": {
          "ark": {
            "type": "string",
            "title": "Ark"
          },
          "reason": {
            "type": "string",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "description": "Why it is being withdrawn. **Required**: someone may be citing it already, and there is no way to know from here."
          },
          "confirm": {
            "type": "string",
            "title": "Confirm",
            "description": "The ARK again, to be sure of the target. Anything else is refused."
          }
        },
        "type": "object",
        "required": [
          "ark",
          "reason",
          "confirm"
        ],
        "title": "UnpublishIn",
        "description": "Withdraw an ARK from publication. The record stays and can be published again; the identifier stops resolving in the meantime. **The name is never reassigned**, so a stale reference gets `404`, never a different object. Because the name has been published, a reason and `confirm` are required."
      },
      "UpdateIn": {
        "properties": {
          "url": {
            "type": "string",
            "title": "Url",
            "default": ""
          },
          "title": {
            "type": "string",
            "title": "Title",
            "default": ""
          },
          "type": {
            "type": "string",
            "title": "Type",
            "default": ""
          },
          "identifier": {
            "type": "string",
            "title": "Identifier",
            "default": ""
          },
          "format": {
            "type": "string",
            "title": "Format",
            "default": ""
          },
          "relation": {
            "type": "string",
            "title": "Relation",
            "default": ""
          },
          "source": {
            "type": "string",
            "title": "Source",
            "default": ""
          },
          "commitment": {
            "type": "string",
            "title": "Commitment",
            "default": ""
          },
          "metadata": {
            "type": "string",
            "title": "Metadata",
            "default": ""
          },
          "who": {
            "type": "string",
            "title": "Who",
            "default": ""
          },
          "when": {
            "type": "string",
            "title": "When",
            "default": ""
          },
          "ark": {
            "type": "string",
            "title": "Ark"
          }
        },
        "type": "object",
        "required": [
          "ark"
        ],
        "title": "UpdateIn",
        "description": "Input for a replacing update. **Every omitted field takes its default**, so send the whole record; to change one field and leave the rest alone, PATCH."
      },
      "ValidationError": {
        "properties": {
          "loc": {
            "items": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "integer"
                }
              ]
            },
            "type": "array",
            "title": "Location"
          },
          "msg": {
            "type": "string",
            "title": "Message"
          },
          "type": {
            "type": "string",
            "title": "Error Type"
          },
          "input": {
            "title": "Input"
          },
          "ctx": {
            "type": "object",
            "title": "Context"
          }
        },
        "type": "object",
        "required": [
          "loc",
          "msg",
          "type"
        ],
        "title": "ValidationError"
      }
    },
    "securitySchemes": {
      "oauth2": {
        "type": "oauth2",
        "description": "Tokens arkhe issues itself (RFC 6749 §4.4 client_credentials).",
        "flows": {
          "clientCredentials": {
            "scopes": {
              "ark:mint": "Mint",
              "ark:update": "Change where it points",
              "ark:read": "Read",
              "ark:tombstone": "Declare it lost",
              "ark:hold": "Suspend forwarding",
              "ark:import": "Import a name minted elsewhere",
              "ark:delete": "Delete one not currently published",
              "ark:unpublish": "Withdraw from publication",
              "ark:purge": "Purge a published one in one step"
            },
            "tokenUrl": "/oauth/token"
          }
        }
      },
      "bearer": {
        "type": "http",
        "description": "An API key (apikey mode), a token arkhe issued (oauth2 mode), or a JWT from an external authorisation server (oidc mode). Which mechanisms are live is set by ARKHE_AUTH.",
        "scheme": "bearer"
      }
    }
  },
  "tags": [
    {
      "name": "ark",
      "description": "Minting and updating. **A write never reaches outside the caller's registered reach.**"
    },
    {
      "name": "resolve",
      "description": "Resolution, with the `?` (a brief description), `??` (the persistence statement), `?info` (for a person) and `?json` (for a program) inflections. **A description can be answered even when the object cannot be reached** (FAIR A2). Present only when started with `ARKHE_RESOLVER=1`."
    }
  ]
}
